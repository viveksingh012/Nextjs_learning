import React from "react";

export default function server(params) {
    return <div>
         <h1>Hello this is server page where the behavior is server side by using "defualt nothing be use something"</h1>
    </div>
}